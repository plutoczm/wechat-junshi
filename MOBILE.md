# 手机优先工作台：第一阶段

> 新入口：`python -m mobile`。这是独立于原 `bot.py` 的增量实现，不会启动旧的 Windows 自动发送程序。适合先在手机浏览器里整理人物记忆、取得可编辑的文字建议。
>
> **这不是已经接通微信的手机机器人。手机历史读取、B 自动发送、实时梗库尚未实现；C 草稿工作流和已导入记录的长期保存已经实现。**

## 当前能力与边界

| 能力 | 本阶段状态 |
| --- | --- |
| 手机浏览器界面 | 已实现响应式页面，后端运行在你自己的电脑/服务器 |
| 同一人的多个微信号 | 手动建立账号标识，勾选后绑定同一人物；原始消息保留来源 |
| 不同人物隔离 | 数据库查询按人物限定，不依赖提示词自觉隔离 |
| 合并、拆分 | 调整账号归属，原始记录不改写，受影响的待确认草稿失效并清空 |
| 长期保存与检索 | SQLite 持久化；中文双字/英文关键词检索 + 最近消息；可手动维护重要背景 |
| C 模式 | 粘贴消息、生成文字、编辑、复制、丢弃、手动确认已发送 |
| B 模式 | 明确禁用；API 请求 B 返回 409，不伪装发送成功 |
| 手机微信历史自动导入 | 尚未接通；当前只接收本文格式的已取得文本 |
| 微信好友列表读取 | 尚未实现；界面中的账号为用户手动建立的标识，不是已验证 wxid |
| DeepSeek | 官方 API `deepseek-flash`，显式关闭思考模式，严格 JSON 输出，只用最终 content |
| 表情包 | 仅支持未知表情的文本占位；不识图、不上传图片、不发送表情包 |
| 自动摘要、语义向量记忆 | 尚未实现；不能把当前关键词检索描述为完整语义记忆 |
| 国内实时梗库 | 尚未接入，不将旧热词或模型猜测冒充实时来源 |

用户报告的桌面版本是 4.1.13.65。wxauto 免费版官方安装页目前列出的支持上限是 4.1.8.107，因此本阶段没有导入 wxauto，也没有要求降级或引入付费适配器。后续需要确认手机为 Android 还是 iOS，再验证对应的免费开源接入路线。手机历史在微信中可见，不代表本工作台已能读取。

原 `bot.py`、`archive.py`、`junshi.py`、原配置与启动脚本保持不变。旧 README 的版本限制和行为属于旧入口，新入口以本文为准；旧队列按 profile 分组等问题也没有被本阶段悄悄修复。请勿将旧入口视为已接通手机工作台。

## 安装与本机试用

使用 Python 3.10–3.13，独立安装移动工作台依赖，不需要安装 `requirements.txt` 中的 wxauto4。

```powershell
git fetch origin
git switch feat/mobile-memory-workbench
python -m venv .venv-mobile
.\.venv-mobile\Scripts\Activate.ps1
python -m pip install -r requirements-mobile.txt

# 在你自己的电脑中生成并保管令牌，不要发到聊天或 GitHub。
$env:JUNSHI_ADMIN_TOKEN = python -c "import secrets; print(secrets.token_urlsafe(32))"
# 为避免终端历史保存密钥，可从现有本地密钥管理器设置此变量。
# $env:DEEPSEEK_API_KEY = <your locally supplied key>
python -m mobile
```

在运行后端的电脑浏览器打开 `http://127.0.0.1:8787`，输入 `JUNSHI_ADMIN_TOKEN` 解锁。没有设置 DeepSeek 密钥时仍能管理和导入记忆，生成建议会明确报告未配置密钥。管理令牌与 DeepSeek 密钥是不同的秘密；不要把 API 密钥填到网页登录框。

Linux/macOS 使用同名环境变量，创建虚拟环境后执行 `source .venv-mobile/bin/activate`。后端为单人、单进程设计，不要用多个 worker 绕过单进程生成并发限制。

| 环境变量 | 作用 |
| --- | --- |
| `JUNSHI_ADMIN_TOKEN` | 至少 32 字符的高熵管理令牌，所有私有 API 均需认证 |
| `DEEPSEEK_API_KEY` | 仅由后端读取的官方 API 密钥 |
| `JUNSHI_DATA_DIR` | 默认 `~/.wechat-junshi`，数据库放在代码仓库外 |
| `JUNSHI_PUBLIC_ORIGIN` | 浏览器实际访问的唯一 origin；远程访问必须是 HTTPS |
| `GOUTOUJUNSHI_SKILL_DIR` | 可选，上游本地知识库目录；只读必要参考，不执行上游记忆脚本 |

DeepSeek API 是按服务商规则计费的模型服务；免费开源的代码和接入依赖不代表模型调用免费。本阶段不把密钥发送到任何中转服务，不自动重试计费请求。

## 手机访问：需要在线后端，不是手机独立运行

第一阶段的运行方式是 **手机浏览器 → 你私有的 HTTPS 入口 → 本机/服务器上的工作台**。电脑关机或后端停止后，手机页面不能继续生成建议。服务运行在服务器时，所谓“本地数据库”指服务器本地，不是手机本地。

远程访问前，需要配置可信的 HTTPS 反向代理和访问网络；仅设置 `JUNSHI_PUBLIC_ORIGIN` 不会自动生成证书或启动 TLS。建议让 Python 后端始终只监听 `127.0.0.1:8787`，由同一主机的 HTTPS 代理转发，并在网络边界限制访问。不要把 8787 明文端口直接开放到公网，不要关闭证书验证。

例如已有域名和可信 HTTPS 代理时，将 origin 设为 `https://junshi.example.com`，代理保留对应 Host。该域名只是占位示例，仓库不会替你创建域名、证书或公开服务。实际部署需要你的主机和网络信息。原生剪贴板功能需要浏览器授权和安全上下文；不可用时可手动选择草稿文字复制。

## 日常使用

先创建一个人物，例如“人物 A”，再添加“她-大号”和“她-小号”两个账号标识。两个号绑定同一人物后，检索会共用该人物下已导入的聊天；其他人物不会被加入模型上下文。合并不会把两个账号的待回复消息混在一起：草稿始终关联选中的账号。

选择当前正在聊天的号，逐号决定是否允许其必要文字片段发送给 DeepSeek，再粘贴对方消息生成建议。另一个号没有打开文字外发授权时，即使同属一个人物，其记录和备注也不会加入云端请求。系统只上传受限的检索片段，而不是每次上传整个数据库。

草稿可以编辑、复制或丢弃。“我已在微信发送这些文字”只是记录**你的手动报告**，不会操作微信，也不是微信发送回执。只有明确报告已发送的文字才作为你的发言加入记录，标记为 `user_confirmed_sent`。复制按钮本身不会标记已发送。

收到新消息、修改备注、变更授权、合并或拆分账号后，旧的待确认草稿会失效。模型请求发出后无法保证撤回；若生成期间发生状态变化，返回结果不会保存为可用草稿。

## 历史文本导入格式

当前导入器不是微信备份解密器，也不直接支持原 `archive.py` 的旧 JSONL。需要先将你有权处理的数据转换为以下 JSONL（每行一个消息对象），或相同对象组成的 JSON 数组。一次选择一个来源账号，再预览和确认导入。一次最多 2000 条，HTTP 请求最大 2 MiB；网页文件选择更保守地限制为 1.5 MB。

以下内容是合成示例，不是真实聊天：

```jsonl
{"id":"demo:1","role":"friend","kind":"text","content":"我不喜欢临时改约","occurred_at":"2026-09-20T20:30:00+08:00"}
{"id":"demo:2","role":"self","kind":"text","content":"好，我们提前商量","occurred_at":"2026-09-20T20:31:00+08:00"}
{"id":"demo:3","role":"friend","kind":"sticker","content":"[表情内容未知]","occurred_at":null}
```

`role` 仅允许 `friend/self/system`，`kind` 仅允许 `text/sticker`（默认 text）。原始时间未知时用 null，不将导入时间冒充发言时间。`content` 原文及空白保留，单条最多 20000 字符；当前待回复消息最多 4000 字符。

优先提供来源内稳定且带来源前缀的 `id`。同一账号下相同 ID 与相同内容重复导入会跳过；相同 ID 内容改变时整批拒绝，不覆盖原记录。相同文字但不同 ID 的“晚安”会分别保留。没有 ID 时使用文件批次标识加行号，保证同一批次重放不重复；**不同文件或重新排版的无 ID 数据不能保证跨文件去重**，不要把这种情况描述为消息级稳定去重。

预览 digest 绑定账号和规范化数据；修改内容或切换账号后必须重新预览。导出只包含选中账号，保持来源 ID，能够重新导入。分批导入可重试已经完成的批次，不等于可以自动从手机断点读取。

原档案丢失的重复消息、未知的原始时间无法靠迁移凭空恢复。后续旧格式适配应把 `ts` 视为旧程序的观察时间，保留真实来源账号，不能直接当作原始发送时间。

## 隐私与删除

数据库默认放在仓库之外，代码不上传真实记录到 GitHub。SQLite 本身未启用数据库加密；请使用操作系统的磁盘加密、用户目录权限和自己的加密备份。不要把工作台管理令牌放进 URL，也不要共享整个浏览器会话。

网页令牌只保存在当前页面内存，不使用 localStorage，不缓存聊天页面；刷新或锁定后需要重新解锁。API 校验 origin/Host、限制请求体、使用参数化查询、限制输出字段，并避免在验证错误中回显输入。页面用 textContent 渲染聊天，不执行聊天中的 HTML。没有安装第三方网页脚本或给模型提供执行工具。

删除消息会删除其检索词、明确绑定到该消息的备注，以及相关人物的草稿快照。手动输入且没有绑定原消息的备注是独立数据，需另外删除；删除账号会删除该账号全部记录和备注。删除不会影响微信原记录、你已导出的文件、历史备份或服务商已经收到的请求。`secure_delete` 不是对 SSD、系统快照和备份的取证级擦除保证。

模型可能误解语气或上下文。C 模式要求你先看再发；重要承诺、不确定事实以及真实情绪不应由模型擅自编造。聊天和上游参考只作为不可信引用数据，不应提升为系统指令。

## 测试与验收

```sh
python -m pip install -r requirements-mobile.txt pytest
python -m pytest -q tests/test_mobile.py
python -m compileall -q mobile
node --check mobile/static/app.js

# 可选：离线浏览器 DOM + ASGI 测试，模型/剪贴板为测试替身
python -m pip install playwright
python -m playwright install chromium
python tests/browser_smoke.py
```

测试覆盖人物隔离、中文旧记录召回、逐账号授权、稳定 ID/重复文本、导入事务/时间、合并拆分、草稿与已发送分离、撤销授权竞争、模型 JSON 错误、API 认证、请求来源、删除级联与导出重放。测试只使用合成数据与 mock 模型，不花费 API 额度、不连接真实微信。

DOM 测试直接加载本地页面并连接 ASGI 测试客户端，测试替身替代剪贴板和 UUID；它不证明真实手机、HTTPS 代理、原生剪贴板或微信桥接可用。真实模型凭据、Android/iOS 真机、手机完整历史和 4.1.13.65 客户端尚需后续现场验收。GitHub Actions 状态以对应 PR 实际结果为准。

## 下一阶段

先确认手机系统以及是否接受常驻电脑/服务器，再验证免费开源采集与发送方案；不要以收费接口、伪造成功响应或无说明降级代替需求。B 模式接通之前必须有按账号独立的收件游标/缓冲、白名单、人工接管、发送回执与结果不明状态，不能盲目重复发送。

后续增加原档案转换、受来源约束的事件/摘要记忆、可编辑人物偏好、公共梗数据源与新鲜度记录。公共梗库必须与私聊库分离；查梗不上传完整聊天，获取失败不阻塞普通回复。

## 官方资料与上游

- [wxauto 安装与版本兼容](https://docs.wxauto.org/docs/install.html)
- [DeepSeek 模型更新记录](https://api-docs.deepseek.com/updates/)
- [DeepSeek 思考模式与最终内容字段](https://api-docs.deepseek.com/guides/thinking_mode/)
- [上游 goutoujunshi](https://github.com/shengjidaguai-china/goutoujunshi)：沿用其 MIT 许可边界；本模块可选读取用户安装的知识文件，不打包或执行上游工具。

本模块沿用仓库 MIT 许可证，不包含真实聊天、用户密钥、付费微信库或未经许可的媒体素材。
